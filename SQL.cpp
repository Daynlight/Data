#include "SQL.h"

namespace Data {
	SQL::SQL(const char* server_name, const char* user_name, const char* password) {
	};

	SQL::~SQL() {
	};
	
	template <class T>
	T SQL::querry(const char* querry) {
	};
};