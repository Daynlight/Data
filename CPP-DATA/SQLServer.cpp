#include "SQLServer.h"

namespace Data {
	SQLServer::SQLServer(const char* server_name, const char* user_name, const char* password) {
	};

	SQLServer::~SQLServer() {
	};
	
	template <class T>
	T SQLServer::querry(const char* querry) {
	};
};